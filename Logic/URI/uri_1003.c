#include <stdio.h>
 
int main() {
	int age,days,month,year;
	scanf("%d", &age);
	if (age = 365){
		printf("%d", &year);
	}
	
	if (age = 30){
		printf("%d", &month);	
	}

	if (age < 30){
		printf("%d", &days);
	}
	
    return 0;
}
